services:
  - type: web
    name: ghs-entrance-exam-api
    env: node
    plan: free
    buildCommand: npm run predeploy && npm install && npm run build
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: FRONTEND_URL
        value: https://ghs-entrance-exam-app.netlify.app
 